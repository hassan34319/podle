/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.sanity.io",
      "www.pexels.com",
      "thinksport.com.au"
    ],
  },
};

module.exports = nextConfig;
